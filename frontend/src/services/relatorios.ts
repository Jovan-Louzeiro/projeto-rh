export type RelatorioTipo = "servidores" | "ferias" | "licencas" | "quinquenios" | "contratos" | "frequencia" | "admissoes" | "desligamentos";
export type FiltrosRelatorio = Record<string, string>;
export type OpcoesRelatorio = { servidores?: string[]; setores?: string[]; cargos?: string[]; situacoes?: string[]; tipos?: string[] };
export type PreviaRelatorio = { colunas: Array<{ chave: string; titulo: string }>; linhas: Array<Record<string, string | number | null>>; totais?: Array<{ titulo: string; valor: string | number }> };
const apiUrl = (import.meta.env.VITE_API_URL ?? "").replace(/\/$/, ""); const endpoint = `${apiUrl}/api/relatorios`;
export async function obterOpcoesRelatorio(tipo: RelatorioTipo, signal?: AbortSignal): Promise<OpcoesRelatorio> { const response = await fetch(`${endpoint}/${tipo}/filtros`, { headers: { Accept: "application/json" }, signal }); if (!response.ok) throw new Error("Não foi possível carregar os filtros."); return response.json(); }
export async function gerarPreviaRelatorio(tipo: RelatorioTipo, filtros: FiltrosRelatorio): Promise<PreviaRelatorio> { const response = await fetch(`${endpoint}/${tipo}/previa`, { method: "POST", headers: { "Content-Type": "application/json", Accept: "application/json" }, body: JSON.stringify({ filtros }) }); if (!response.ok) throw new Error("Não foi possível gerar o relatório."); return response.json(); }
export async function exportarPdfRelatorio(tipo: RelatorioTipo, filtros: FiltrosRelatorio): Promise<Blob> { const response = await fetch(`${endpoint}/${tipo}/exportar?formato=pdf`, { method: "POST", headers: { "Content-Type": "application/json", Accept: "application/pdf" }, body: JSON.stringify({ filtros }) }); if (!response.ok) throw new Error("Não foi possível exportar o PDF."); return response.blob(); }
